import 'package:flutter/material.dart';

/// Premium paywall has been removed — all features are free.
/// This stub exists only to satisfy existing call sites.
Future<void> showPremiumPaywallSheet(
  BuildContext context, {
  required String featureName,
  String? description,
}) async {
  // No-op: all features are freely available.
}
